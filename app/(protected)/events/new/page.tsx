import Link from 'next/link';
import { ArrowLeft, CalendarDays } from 'lucide-react';
import { createEvent } from '../actions';

export default async function NewEvent({searchParams}:{searchParams:Promise<{error?:string}>}){
  const params=await searchParams;
  return <div className="creation-page"><Link className="creation-back" href="/events"><ArrowLeft size={15}/> Voltar para eventos</Link><div className="creation-layout"><section className="creation-intro"><span className="creation-icon"><CalendarDays size={23}/></span><span className="module-kicker">Novo evento</span><h1>Planeje um evento</h1><p>Depois de publicar você poderá acompanhar inscrições, check-in e presença no workspace do evento.</p></section><section className="creation-form-panel">{params.error&&<p className="alert">{params.error}</p>}<form action={createEvent} className="form"><div className="field"><label>Título</label><input name="title" placeholder="Ex.: Culto da noite" required/></div><div className="form-row"><div className="field"><label>Início</label><input name="startsAt" type="datetime-local" required/></div><div className="field"><label>Término</label><input name="endsAt" type="datetime-local"/></div></div><div className="field"><label>Local</label><input name="address" placeholder="Auditório principal"/></div><div className="field"><label>Capacidade</label><input name="capacity" type="number" min="1" placeholder="Ex.: 300"/></div><div className="creation-actions"><Link href="/events">Cancelar</Link><button className="primary-submit" type="submit">Publicar evento</button></div></form></section></div></div>;
}
